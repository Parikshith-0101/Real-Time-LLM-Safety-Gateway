# ML Module

**Owner: Parikshith**

This directory contains ML models, training scripts, and datasets for the LLM Safety Gateway.

## Structure

- `models/` - Trained models (LightGBM, Logistic Regression, etc.)
- `training/` - Training scripts and notebooks
- `datasets/` - Training datasets (red-team samples, attack examples)

## Models

- Lightweight Intent Classifier
- Feature extraction pipelines
- Red-team testing scripts

